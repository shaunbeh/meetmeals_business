stages:
  - develop

deploy-on-develop:
  stage: develop
  script:
    - make build
    - docker compose -p meetmeals-enigma -f /srv/www/meetmeals-backend-server/.deploy/compose/docker-compose.yml up -d frontend
  when: always
  only:
    - develop
  tags:
    - develop
